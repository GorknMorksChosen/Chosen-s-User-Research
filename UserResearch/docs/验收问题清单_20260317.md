# 验收问题清单（2026-03-17）

> 依据 `UserResearch/五剑客_wjxspss目录_映射验收表.md` 执行「一」：跑完 16 条验收 → 出问题清单。  
> 本清单记录**不通过项**及**需人工执行的项**，便于排期修复与后续回归。

---

## 一、自动化可执行部分（质量矩阵）

| 脚本 | 结果 | 说明 |
|------|------|------|
| verify_dependency_matrix.py | 通过 | 依赖矩阵校验正常 |
| test_migration.py | 通过 | 统计迁移一致性通过 |
| verify_current_v1_logic.py | 通过 | v1 核心统计逻辑可用 |
| auto_verify_v1.py | 通过 | 核心 schema 验收（p_value/effect_size/assumption_checks 等，见 DEV_LOG） |
| verify_p2_baseline.py | 通过 | 已支持 example → test_assets 回退；无可用 .xlsx 时跳过并正常退出 |
| verify_p24_clustering.py | 通过 | 同上，路径回退 + 无文件时跳过 |

**结论**：质量矩阵 **6/6 通过**。此前依赖 `UserResearch/example` 的 2 个脚本已改为支持 `test_assets` 回退并在无样本时跳过，不影响 16 条映射验收的独立性。

---

## 二、16 条映射验收执行情况与问题记录

16 条验收均需**人工在界面执行**（运行对应 Streamlit 入口、上传数据、按检查点查看界面/导出）。当前未进行人工逐条点击验收，因此**验收状态仍为「待验收」**。

**映射表条目编号**（与 `五剑客_wjxspss目录_映射验收表.md` 一一对应，不通过时请用编号引用）：

| 编号 | 大类 | 清单条目摘要 |
|------|------|--------------|
| M1 | 数据探索与格式 | 探索数据特征（描述统计、分布） |
| M2 | 数据探索与格式 | 定类数据探索分析 |
| M3 | 数据探索与格式 | 定量数据探索分析 |
| M4 | 数据探索与格式 | 数据分布之正态性分析（检验法） |
| M5 | 问卷数据清理 | 异常值处理 |
| M6 | 问卷数据清理 | 标题处理及其他 |
| M7 | 差异关系研究 | t 检验（多组→ANOVA/Kruskal） |
| M8 | 差异关系研究 | 方差分析（单因素 + 事后比较） |
| M9 | 差异关系研究 | 卡方检验（2×2、R×C、Fisher） |
| M10 | 差异关系研究 | 非参数（Kruskal-Wallis + Dunn） |
| M11 | 相关与影响关系 | 相关分析（IPA 用相关系数） |
| M12 | 相关与影响关系 | 线性回归（多重、VIF、信度） |
| M13 | 问卷专属研究 | 单选题与多选题分析（频数、卡方） |
| M14 | 问卷专属研究 | 信度分析（Cronbach α） |
| M15 | 问卷专属研究 | 路径分析 / 结构方程模型分析 |
| M16 | 其他分析能力 | 聚类分析 / 文本分析 |

**不通过项记录表**（人工验收后填写）：

| 编号 | 清单条目 | 对应五剑客 | 现象 | 可能原因 |
|------|----------|------------|------|----------|
| （待填） | — | — | 人工执行 16 条后，将不通过项按上表格式追加到此表。 | — |

**人工验收时建议关注**（代码审阅已知风险，多数已修复）：排序题导出、换文件仍用旧数据、IPA 空表、缺失策略回退、聚类 recommended_k 类型、CSV 编码、Streamlit rerun 等已按 `CODE_REVIEW_DEBUG_REPORT.md` 完成修复；若验收中仍出现相关现象，请记入上表并注明「与报告第 X 条相关」。

---

## 三、人工验收后的填写说明

1. **执行顺序**：按 `五剑客_wjxspss目录_映射验收表.md` 中 1～6 大类逐条执行，使用 `UserResearch/input_example` 下数据（或等价问卷数据）。
2. **不通过时**：在本文档「二、不通过项记录表」中新增一行，填写**编号（M1～M16）**、清单条目、对应五剑客、**现象**（界面报错/导出缺失/结果与预期不符）、**可能原因**（可选）。
3. **同步更新**：将 `五剑客_wjxspss目录_映射验收表.md` 中该条的「验收状态」改为「不通过」，并更新文末「验收状态汇总」。

---

## 四、与下一步 to-do 的对应

- **下一步_to-do_项目主管.md**「一、立即执行」中：**1. 执行映射验收表** = 人工跑完上述 16 条并更新映射表状态；**2. 验收问题清单** = 将不通过项整理到本文档「二、不通过项记录表」。本次推进已更新「一、质量矩阵」为 6/6，并补全 16 条编号与填写说明，便于后续人工验收后直接填表。

---

## 五、参考文档

- 映射验收表：`UserResearch/五剑客_wjxspss目录_映射验收表.md`
- 下一步 to-do：`UserResearch/下一步_to-do_项目主管.md`
- 需求与规则符合度检查：见同目录下 `需求与规则符合度检查_20260317.md`
